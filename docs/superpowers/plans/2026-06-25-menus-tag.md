# menus-tag Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a `menus-tag` skill that anchors each dish on a canonical English weekly menu to a recipe and assigns a core controlled-vocabulary tag set, written into a deduped `dishes:` frontmatter block plus a derived body table.

**Architecture:** The deliverable is a prompt document (`.claude/skills/menus-tag/SKILL.md`), the menu analogue of the existing `recipes-tag` skill, plus a one-time extension of the canonical menu template (`data/menus/_TEMPLATE.md`) to document the new structure. There is **no executable code and no unit-test harness** in this repo; skills are validated by running them against the real menu files and checking the output against acceptance criteria (the spec). The TDD "red/green" cycle is therefore replaced by: define acceptance criteria → run the skill on a real menu → verify the criteria → commit.

**Tech Stack:** Markdown + YAML frontmatter (skills and data files). Git (Bash tool, POSIX sh) for commits. No package manager, no test framework.

## Global Constraints

These apply to every task; the SKILL.md must encode them and every validation must check them.

- **Process only** canonical English menus in `data/menus/processed/transcribed-en/` whose `status:` is **not** `archived`. Never read or edit the `transcribed-fr/` husks.
- **Controlled vocabulary** comes from `data/recipes/_TEMPLATE.md`, read fresh each run. `cuisine` is the one open field (prefer a listed value, add a lowercase one only if nothing fits); every other tag value must be an allowed value or blank.
- **Core tag set per dish:** `protein, temperature, richness, format, cuisine, spice_level, diet`. No other tag dimensions.
- **Confident-or-blank:** set a tag only when evidence clearly supports one allowed value; otherwise leave it blank.
- **menus-tag writes only** the `dishes:` frontmatter block and the regenerated `## Dish tags` body table. It never edits `pool`, `schedule`, `status`, `flag`, `comment`, identity fields, or the body's other sections, and never moves a file.
- **Idempotent:** re-running adds entries for new dishes and fills only still-blank fields; never overwrite a value already present (a human may have set it). It may upgrade a dish from `tag_source: inferred` to `linked` (replacing only auto-inferred values) when a matching tagged `transcribed-en` recipe later appears.
- **Spec is authoritative:** `docs/superpowers/specs/2026-06-25-menus-tag-design.md`.

---

## File Structure

- **Create:** `.claude/skills/menus-tag/SKILL.md` — the skill prompt document (the bulk of the work).
- **Modify:** `data/menus/_TEMPLATE.md` — add the documented `dishes:` frontmatter block and the `## Dish tags` body section.
- **Validated against (edited in place during validation):** `data/menus/processed/transcribed-en/*.md` — the 6 existing menus.
- **Read-only references the skill depends on:** `data/recipes/_TEMPLATE.md` (vocabulary), `data/parsley/INDEX.md` + `data/parsley/recipes-*/` (repertoire lookup), `data/recipes/processed/transcribed-en/*.md` (tagged recipes to link/copy from).

The two existing skills to mirror for tone, structure, and gating rules are `.claude/skills/recipes-tag/SKILL.md` and `.claude/skills/recipes-translate/SKILL.md`.

---

## Task 1: Extend the canonical menu template

**Files:**
- Modify: `data/menus/_TEMPLATE.md`

**Interfaces:**
- Consumes: nothing.
- Produces: the documented `dishes:` block schema and `## Dish tags` body section that Task 2's skill must match exactly (field names: `name`, `recipe_id`, `recipe_source`, `tag_source`, `protein`, `temperature`, `richness`, `format`, `cuisine`, `spice_level`, `diet`).

- [ ] **Step 1: Define acceptance criteria**

The edited template must: (a) keep every existing field unchanged; (b) add a commented `dishes:` block at the **end of the frontmatter** (after `comment:`); (c) add a `## Dish tags` section at the **end of the body** (after `## Comments and weather`); (d) the new block must carry a header comment explaining that `menus-tag` (not transcription) writes it. The `_TEMPLATE_HUSK.md` file is **not** touched.

- [ ] **Step 2: Add the `dishes:` block to the frontmatter**

In `data/menus/_TEMPLATE.md`, immediately after the `comment:` line (line 58) and before the closing `---`, insert:

```yaml

# ============================================================
# DISH TAGS — added by the menus-tag skill (NOT during transcription).
# One entry per UNIQUE dish across pool + schedule, deduped; `name` must
# match the string used in pool/schedule (markers like "(struck out)" / "?"
# stripped). Tag values use the controlled vocabulary in
# data/recipes/_TEMPLATE.md. menus-tag writes ONLY this block and the body
# "Dish tags" table — never pool/schedule/status.
# ============================================================
dishes: []
#   - name:                 # exact dish string as in pool/schedule, markers stripped
#     recipe_id:            # tagged recipe id | parsley match | blank if none
#     recipe_source:        # transcribed-en | parsley | none
#     tag_source:           # linked (copied from a tagged recipe) | inferred
#     protein:              # see allowed values in data/recipes/_TEMPLATE.md
#     temperature:          # hot | warm | room | cold | frozen
#     richness:             # lean | light | moderate | rich | indulgent
#     format:               # soup | broth | salad | stew | curry | handheld | plated-main | shareable | side | dip-spread | bowl | skewer | pasta | casserole | baked-good | beverage | sauce-condiment
#     cuisine:              # OPEN list — see data/recipes/_TEMPLATE.md
#     spice_level:          # none | mild | medium | hot | very-hot
#     diet: []              # vegan | vegetarian | pescatarian | gluten-free | dairy-free | ...
```

- [ ] **Step 3: Add the `## Dish tags` body section**

At the very end of `data/menus/_TEMPLATE.md` (after the `## Comments and weather` block, currently ending at line 73), append:

```markdown

## Dish tags

<!-- regenerated by menus-tag from the dishes: block — do not hand-edit -->
```

- [ ] **Step 4: Verify**

Re-read `data/menus/_TEMPLATE.md` and confirm: the `dishes:` block sits inside the frontmatter (before the closing `---`), the `## Dish tags` section sits at the end of the body, and no pre-existing field/section was altered. Confirm `data/menus/_TEMPLATE_HUSK.md` is unchanged (not opened/edited).

- [ ] **Step 5: Commit**

```bash
git add data/menus/_TEMPLATE.md
git commit -m "feat(menus): document dishes: tag block + Dish tags section in menu template

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 2: Author the menus-tag SKILL.md

**Files:**
- Create: `.claude/skills/menus-tag/SKILL.md`

**Interfaces:**
- Consumes: the `dishes:` block schema from Task 1; the vocabulary in `data/recipes/_TEMPLATE.md`; the `recipes-tag` skill as a structural model.
- Produces: the executable skill behavior validated in Tasks 3–4. The skill, when run, writes the `dishes:` block + `## Dish tags` table into a menu file.

- [ ] **Step 1: Define acceptance criteria**

The file must have valid YAML frontmatter with `name: menus-tag` and a `description:` that follows the project's trigger style (third-person, says when to use it, says what it does NOT do). The body must specify: inputs/outputs/gating, the two-tier matching procedure, the normalize/filter rules for noise and struck-out items, the core tag set, the confident-or-blank rule, idempotency (including inferred→linked upgrade), and the "only touch the dishes block + body table" rule. It must reference the exact paths in the Global Constraints.

- [ ] **Step 2: Create the skill file with the full content below**

Create `.claude/skills/menus-tag/SKILL.md` with exactly this content:

````markdown
---
name: menus-tag
description: >-
  Add structured menu-planning tags to the canonical English weekly menus in
  data/menus/processed/transcribed-en/. Use when the user wants to tag/classify
  menu dishes, link menu dishes to recipes, fill in the dishes block, or "tag the
  menus". For each unique dish in a week's pool/schedule it anchors the dish to a
  recipe (the tagged transcribed-en recipe if one exists, else the Parsley
  repertoire) and infers a core controlled-vocabulary tag set (protein,
  temperature, richness, format, cuisine, spice_level, diet). This is the menu
  analogue of recipes-tag. Does NOT transcribe, translate, change status, or
  interpret the chef's reasoning — that is the later menus-analyze skill.
---

# Tag menu dishes for menu planning

Fill the `dishes:` block (and regenerate the `## Dish tags` body table) of each
canonical English weekly menu by **anchoring every dish to a recipe where
possible and reasoning about what the dish is**. This is the menu analogue of
`recipes-tag`: the menu transcription step deliberately leaves dishes as
free-text, and this skill is the later pass that links and tags them so a menu
planner/analyzer can later find patterns and build balanced weeks.

This skill **infers** — that is its job — but it infers carefully: **tag only
what you are confident about, and leave the rest blank.** A blank field is
correct; a wrong tag is worse than no tag because downstream tools trust it.

## Inputs and outputs

- **Input:** canonical English menus in `data/menus/processed/transcribed-en/`.
  Process a file only if it has a `status:` field whose value is **not**
  `archived`. **Skip any file with no `status:` field** and **skip
  `status: archived`.** Never read or edit the original-language husks in
  `data/menus/processed/transcribed-fr/`.
- **Vocabulary (schema):** `data/recipes/_TEMPLATE.md` — the controlled
  vocabularies and **allowed values** for every tag. Read it fresh every run; it
  is the source of truth. Use only the core subset (below).
- **Link targets:**
  - `data/recipes/processed/transcribed-en/` — our canonical **tagged** recipes.
    When a dish matches one, its tags can be **copied** verbatim.
  - `data/parsley/INDEX.md` + `data/parsley/recipes-en/` and
    `data/parsley/recipes-fr/` — the chef's ~210-recipe repertoire (untagged,
    mostly French). Use for dish identity and as ingredient evidence to inform
    inference.
- **Output:** the same `transcribed-en/` menu file, edited in place. Only ever
  write the `dishes:` frontmatter block and regenerate the `## Dish tags` body
  table. Never alter `id`, `week_of`, `pool`, `schedule`, `status`, `flag`,
  `comment`, or any other field, and never move a file.

## The core tag set

Per dish, fill only these (allowed values in `data/recipes/_TEMPLATE.md`):

- `protein` — the dish's main component. Watch for meat substitutes (vege
  chicken / TVP / pulled mushroom → `plant-substitute`), mixed mains (`mixed`),
  and meatless dishes (`vegetable`/`legume`/`grain`/`cheese`/`none`).
- `temperature` — `hot` | `warm` | `room` | `cold` | `frozen`. A simmered soup
  is `hot`; a composed salad or gazpacho is `cold`.
- `richness` — cream/butter/cheese/frying/fatty cuts push `rich`/`indulgent`;
  broths, lean proteins, vegetables push `light`/`lean`.
- `format` — `soup` | `salad` | `handheld` | `plated-main` | `dip-spread` |
  `bowl` | `curry` | `pasta` | `sauce-condiment` | … (full list in the template).
- `cuisine` — the open field. Prefer a listed value; add a new lowercase one
  only when nothing fits.
- `spice_level` — `none` | `mild` | `medium` | `hot` | `very-hot`, from
  chili/heat ingredients. If unsure, leave blank.
- `diet` (multi) — list every claim the evidence **clearly supports**
  (`vegan`/`vegetarian`/`pescatarian`/`gluten-free`/`dairy-free`/…). Be
  conservative: if an opaque "maison" powder or sub-recipe could hide an animal
  product or gluten, do not claim the restrictive diet.

Do **not** add `course`, `weight`, `season`, `texture`, or `flavor_profile` —
they are intentionally out of scope for menu tagging.

## Procedure

1. **Find work.** List menus in `data/menus/processed/transcribed-en/`. A menu
   needs tagging if it has a non-`archived` `status:` and its `dishes:` block is
   missing, empty, or incomplete. By default process every such menu; if the
   user names specific weeks, do only those.

2. **Load the vocabulary.** Read `data/recipes/_TEMPLATE.md` and use the comment
   after each tag field as the allowed-value list. Read `data/parsley/INDEX.md`
   so you can look up repertoire recipes by name.

3. **For each menu, in turn:**

   a. **Collect dishes.** Take the union of every dish string across `pool`
      (all categories) and `schedule[].dishes`.

   b. **Normalize & filter.** Strip status/uncertainty markers to get the clean
      dish name: trailing `?`, parentheticals like `(struck out)` / `(struck)`,
      and quantity notes. **Drop pure non-dish tokens** — e.g. `x3`, `Yes`,
      `Yes ? — if cheap`, `MDF ?`, bare category placeholders. Dedup to unique
      clean names (a dish appearing on several days gets ONE entry). Whether a
      dish was struck out or uncertain is a scheduling fact for the later
      `menus-analyze` skill — it is **not** a tag here.

   c. **Two-tier match (per unique dish):**
      - **Tier 1 — tagged canonical recipe.** If the dish confidently matches a
        recipe in `data/recipes/processed/transcribed-en/` (by title/name,
        across FR↔EN), set `recipe_id` to that file's `id`,
        `recipe_source: transcribed-en`, **copy** its core tags, and set
        `tag_source: linked`.
      - **Tier 2 — Parsley repertoire.** Else, if it confidently matches a
        Parsley recipe (via `INDEX.md`; match across FR↔EN, e.g. "Pho" →
        "Soup Pho Végane", "Kale mango cabbage" → "Salade Kale Mangue Chou"),
        set `recipe_id` to a kebab-case id derived from that recipe's name,
        `recipe_source: parsley`, **read that recipe's ingredients**, infer the
        core tags from the name + ingredients, and set `tag_source: inferred`.
      - **Tier 3 — name only.** Else infer the core tags from the dish name
        alone; set `recipe_id:` blank, `recipe_source: none`,
        `tag_source: inferred`.

   d. **Confident or blank.** For each tag, set an allowed value only when the
      evidence clearly supports it; otherwise leave it blank. Never overwrite a
      value already present (a human may have set it) — fill only blanks. You
      may upgrade a dish from `inferred` to `linked` (replacing only
      auto-inferred values, never human edits) when a Tier-1 match now exists.

   e. **Write the block.** Write/extend the `dishes:` block: one entry per unique
      dish, with `name`, `recipe_id`, `recipe_source`, `tag_source`, and the core
      tags. Keep YAML consistent with `data/menus/_TEMPLATE.md` (scalars for
      single-value fields, lists for `diet`).

   f. **Regenerate the body table.** Replace (or create) the `## Dish tags`
      section with a table built **from the `dishes:` block** — one row per dish,
      columns: Dish | Protein | Temp | Richness | Format | Cuisine | Spice |
      Diet | Recipe. Use `—` for blank cells. The table is a derived view: always
      regenerate it from the block so the two cannot drift.

4. **Report.** Per menu, summarize: how many dishes tagged, links found broken
   down by source (`transcribed-en` / `parsley` / `none`), and which dishes were
   left mostly blank because they were ambiguous or unmatched.

## Rules

- **Infer from content, not the title alone.** A "poulet" that is mushroom-based
  TVP is `plant-substitute`; a "salade" served warm is not `cold`. When a
  Parsley match exists, read its ingredients before tagging.
- **Confident or blank.** Set a tag only when clearly supported; downstream
  tools trust these tags.
- **Controlled vocabulary only.** Every value comes from the allowed list in
  `data/recipes/_TEMPLATE.md`; `cuisine` is the one open field.
- **Mark trust.** Every dish carries `recipe_source` and `tag_source`.
- **Never overwrite, only fill.** Re-running fills still-blank fields and adds
  entries for new dishes. Idempotent and safe to re-run.
- **Touch the dishes block + body table only.** Never change `pool`, `schedule`,
  `status`, `flag`, `comment`, identity fields, or move a file.
- **English canonical only.** Process only `transcribed-en/` menus with a
  non-`archived` `status:`. Skip husks entirely.
- **Be conservative with safety claims.** Do not assert a restrictive `diet`
  when a "maison" powder, sauce, or sub-recipe could hide an animal product or
  allergen.
````

- [ ] **Step 3: Verify the file structure**

Re-read `.claude/skills/menus-tag/SKILL.md`. Confirm: frontmatter has `name: menus-tag` and a `description:`; the body contains the Inputs/outputs, Core tag set, Procedure (with the three tiers), and Rules sections; every path referenced (`data/menus/processed/transcribed-en/`, `data/recipes/_TEMPLATE.md`, `data/parsley/INDEX.md`) exists. Spot-check that the field names match Task 1's block exactly (`recipe_source`, `tag_source`, etc.).

- [ ] **Step 4: Commit**

```bash
git add .claude/skills/menus-tag/SKILL.md
git commit -m "feat(menus-tag): add skill that links + tags menu dishes

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 3: Validate on the hardest menu (2026-06-15)

This is the acceptance test. The 14–20 June menu is the richest case: it has links to tagged recipes, Parsley-only dishes, struck-out items, non-dish tokens (`x3`, `Yes`, `MDF ?`), temperatures, and weather.

**Files:**
- Modify (skill output): `data/menus/processed/transcribed-en/2026-06-15.md`

**Interfaces:**
- Consumes: the SKILL.md from Task 2.
- Produces: a fully tagged 2026-06-15 menu used as the reference example.

- [ ] **Step 1: Capture the "before" of the protected sections**

Record the current `pool:`, `schedule:`, `status:`, and `comment:` of `data/menus/processed/transcribed-en/2026-06-15.md` (read them) so you can confirm afterwards they are byte-for-byte unchanged.

- [ ] **Step 2: Run the skill on this one menu**

Invoke the `menus-tag` skill targeting only `2026-06-15`. Let it produce the `dishes:` block and `## Dish tags` table.

- [ ] **Step 3: Verify the output against acceptance criteria**

Re-read `data/menus/processed/transcribed-en/2026-06-15.md` and confirm ALL of:
- **Dedup:** dishes appearing in both `pool` and `schedule` (e.g. "Salmon pesto zucchini", "Pho") have exactly ONE entry in `dishes:`.
- **Noise dropped:** no `dishes:` entry for `x3`, `Yes`, `MDF ?`, or other non-dish tokens.
- **Struck-out normalized:** "Chicken (struck out)" / "Pho (struck out)" appear (if at all) under their clean name ("Chicken", "Pho"), with no strike marker and no extra status field.
- **Trust fields present:** every entry has both `recipe_source` (`transcribed-en`|`parsley`|`none`) and `tag_source` (`linked`|`inferred`).
- **Vocabulary:** every non-blank tag value is an allowed value from `data/recipes/_TEMPLATE.md` (cuisine may be a new lowercase value).
- **Body table:** a `## Dish tags` table exists, has one row per `dishes:` entry, and its values match the block (blanks shown as `—`).
- **Protected sections untouched:** `pool`, `schedule`, `status`, `flag`, `comment` are identical to the Step 1 capture.

Run to confirm only the intended regions changed:
```bash
git --no-pager diff -- data/menus/processed/transcribed-en/2026-06-15.md
```
Expected: additions are the `dishes:` block and the `## Dish tags` section only; no lines removed from `pool`/`schedule`/`comment`.

- [ ] **Step 4: Fix the skill if criteria fail**

If any criterion fails, the fault is in the SKILL.md wording (the procedure was ambiguous). Revert the menu (`git checkout -- data/menus/processed/transcribed-en/2026-06-15.md`), edit `.claude/skills/menus-tag/SKILL.md` to remove the ambiguity, commit the skill fix, and re-run Steps 2–3.

- [ ] **Step 5: Commit the tagged menu**

```bash
git add data/menus/processed/transcribed-en/2026-06-15.md
git commit -m "data(menus): tag 2026-06-15 dishes via menus-tag (reference example)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 4: Idempotency check + full run

**Files:**
- Modify (skill output): the remaining `data/menus/processed/transcribed-en/*.md` menus.

**Interfaces:**
- Consumes: the validated skill and the tagged 2026-06-15 menu.
- Produces: all 6 menus tagged; confirmation the skill is idempotent.

- [ ] **Step 1: Idempotency — re-run on the already-tagged menu**

Run `menus-tag` again on `2026-06-15` (already fully tagged). Then:
```bash
git --no-pager diff -- data/menus/processed/transcribed-en/2026-06-15.md
```
Expected: **no changes** (empty diff). A fully-tagged menu re-run must be a no-op. If the diff is non-empty, the "never overwrite, only fill" rule is being violated — fix the SKILL.md wording, commit the fix, revert the menu, and recheck.

- [ ] **Step 2: Run on the remaining five menus**

Invoke `menus-tag` on the other weeks (`2026-05-11`, `2026-05-18`, `2026-05-25`, `2026-06-01`, `2026-06-08`).

- [ ] **Step 3: Spot-verify two menus**

For `2026-05-11` and one other, re-read the file and confirm the same acceptance criteria as Task 3 Step 3 (dedup, noise dropped, trust fields, vocabulary, body table, protected sections untouched). Confirm cross-week consistency: a recurring dish like "Pho" or "KM" (Kale mango cabbage) gets the same core tags it received in 2026-06-15.

- [ ] **Step 4: Commit the remaining tagged menus**

```bash
git add data/menus/processed/transcribed-en/2026-05-11.md data/menus/processed/transcribed-en/2026-05-18.md data/menus/processed/transcribed-en/2026-05-25.md data/menus/processed/transcribed-en/2026-06-01.md data/menus/processed/transcribed-en/2026-06-08.md
git commit -m "data(menus): tag remaining weekly menus via menus-tag

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Notes for the implementer

- This repo has **no test runner, linter, or build**. Do not invent one. "Verify" means read the file and check the listed criteria, plus `git diff` to confirm change scope.
- The hardest part is FR↔EN name matching against the Parsley `INDEX.md`. When a match is not confident, prefer Tier 3 (infer from name, `recipe_source: none`) over a wrong link — a wrong `recipe_id` is worse than none.
- Keep the skill's tone and section shape close to `.claude/skills/recipes-tag/SKILL.md`; they are sibling skills and consistency matters.
- `menus-analyze` (chef-intent / pattern analysis / recommendations) is a **separate future skill** and explicitly out of scope here.
